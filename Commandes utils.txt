### Installapip install -r requirements.txttion (local)

# (optionnel) créer un environnement virtuel
python -m venv .venv

# macOS / Linux
source .venv/bin/activate

# Windows (PowerShell)
# .venv\Scripts\Activate.ps1




### Lancer l'application Streamlit

streamlit run app.py


### Données utilisées

# Par défaut l'app cherche le fichier Excel ici (dans le repo parent) :
# ../data should be used/Risque_data.xlsx
#
# Vous pouvez aussi uploader un fichier .xlsx ou .csv depuis l'UI.


### (Optionnel) Docker

# build
# docker build -t streamlit-app-v1 .
#
# run
# docker run -p 8501:8501 streamlit-app-v1

